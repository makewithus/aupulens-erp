# Workspace Rules

## Git Workflow
- **NEVER** run `git push` autonomously.
- You must ONLY stage and commit files locally when asked.
- You may suggest that the user push the code or ask for explicit permission, but you must NOT push code yourself without explicit instruction from the user.
