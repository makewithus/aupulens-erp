# AUPULENS — SARVAM, PHASE 4 (CLOSE-OUT) — saved in substance; branch `sarvam`, commit locally, NEVER push/merge
Key now in .env. Real Sarvam call = main work. Build Part 5 list first.
## Part 0 live API
0.1 CI stays mocked always; live checks = separate explicitly-invoked script (scripts/sarvam-live-check.ts), never in the suite. Key only in .env — never logs/errors/committed output/docs; check script output for leakage before running. Credits finite: plan, batch, no loops, record approx call count. If live contradicts a mock, the MOCK is wrong: fix mock, re-run suite, record.
0.2 Order: (1) placeholder format ZXQnZXQ survives translation in several languages — else try alternatives, change the one constant, re-run; do FIRST. (2) round-trip semantic check EN→lang→EN for all 11 languages; per-language caveats. (3) real LIVE_VERIFICATION phrases through detection+translation; failures flagged "needs native speaker", never soften expectations. (4) code-mixed + WhatsApp-damaged input. (5) protected entities vs real API (typo-looking company name, invoice number, GSTIN, quoted deliberate misspelling). (6) failure paths for real: invalid key, malformed request, oversized payload, forced timeout — always degrade, never surface error. (7) uncached regional latency p95 over realistic sample vs 1.5s budget (per language if material). (8) classification prompt vs real Azure model: uncertain phrasings ("can you raise a bill for Acme" etc.) correct and stable.
0.3 Fix everything revealed; record each in LIVE_VERIFICATION.md as expected/observed/action.
## Part 1 production browser pass
Re-run all 34 steps against `npm run build:local` + `npm run start` with the real key; key steps live not mock (regional guided flow to prefilled form, "I understood this as…", auto-create, provider-down by pulling the key mid-run). QA_GUIDE self-run log: "browser, production build", live vs mocked marked.
## Part 2 docs/sarvam/SARVAM_TEST.md
Manual confidence pass for the user (30–45 min, no terminal beyond setup). Setup (env, seed, login, demo org, reset — one copy-paste block); what's built (half page); mocked vs live (after Part 0); tests in order, each: type / should happen / look for / wrong — Groups A English unchanged, B English guided (summary-only, gaps, escape, cancel, resume), C Regional (Hindi, Tamil, code-mixed, Roman), D Messy (WhatsApp paste, typos, 45k, quoted misspelling survives), E Protection (typo-looking name, invoice no., GSTIN), F Admin (cost split, Configuration, switch off → clean fallback), G Failure (remove key, English works, degrades). Then "what would be a real problem", "knowingly not done". Every step run by me first; never write an unobserved expectation.
## Part 3
PERFORMANCE.md real uncached latency + English delta on production build; STATUS.md no pending cells (verified / declared / named open item with owner); leave the 2 eslint errors+1 warning in tests/ai/aiRuntime/safety.test.ts & ai29…EdgeCases (belong to ai/workflows) and note in merge notes; re-run merge dry-runs, confirm order global/admin → ai/workflows → sarvam.
## Part 4
Full suite 3× in fresh worktree off final commit (BASELINE command), identical; tsc, eslint on touched, next build clean; clean tree; production browser pass 34/34; live API 8 steps all actioned; every SARVAM_TEST step run first.
## Rules
Azure stays brain · additive (vitest.config.ts the one exception) · English path must not regress · never autocorrect input · protected entities never altered · fail open · never act on a guess for financial · every provider call metered · key never leaves .env · CI mocked · suite green vs baseline · never push/merge. Ask only if live API differs so much that the pipeline design (not tuning) must change (e.g. placeholders cannot survive in any form).
When live disagrees with a mock: the mock changes, NOT the expectation.
