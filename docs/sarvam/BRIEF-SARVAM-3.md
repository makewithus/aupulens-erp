# AUPULENS — SARVAM, PHASE 3 (FINAL) — saved in substance; branch `sarvam`, commit locally, NEVER push/merge
Feature 3 accepted. Build the Part 5 list first, in order.

## Part 0 decisions
0.1 Item name: KEEP ASKING (never default "Services" — customer-facing financial document). Offer that tenant's recent/most-used item names (from their real invoice history) as choices; free text still allowed.
0.2 English create = guided flow, SHIP with two guards: (a) skip-ahead — if nothing mandatory is missing go straight to the summary, no questions ("Create an invoice for Acme, 45000, due 30 days, consulting services" ⇒ summary only); (b) escape hatch at every question: "Skip the questions and open the form" ⇒ today's behaviour (partial pre-filled form). VERIFY unusual phrasings classify: "can you raise a bill for Acme", "need an invoice, Acme, 45k", "acme invoice pls"; if deterministic classification is uncertain, fall back to an LLM classification rather than ignore a clear instruction. Test real-world phrasings.
0.3 vitest timeouts: approved with trim — try 30s hook / 15s test; use 45/20 only if 30/15 still flakes; record choice+evidence in BASELINE.md. Flag prominently for the wider team (affects every suite; flakiness pre-dated branch on both trees; revertible).
## Part 1 escalations (don't fix, make impossible to miss)
1.1 Azure cost cap NOT enforced (AiLimit.maxCostUsdPerMonth stored/presented, does nothing for Azure): own heading in STATUS.md, docs/admin/OPEN_QUESTIONS.md, merge notes; what it implies / what it does / shape of fix (extend the Sarvam gate to tenantAi.ts Azure path). One paragraph.
1.2 500 on customerless draft invoice: pre-existing; PROVE the guided flow can never produce it (test driving flow to completion without a customer asserts it cannot); add assistant-side defensive check if any path could.
## Part 2 browser verification (real browser, demo tenant)
Platform dashboard Azure/Sarvam split + combined; org AI Usage provider split; Configuration tab multilingual status/languages/switch incl. toggling + audit record; chat: English create with everything supplied (summary only), with gaps (guided), skip-questions escape, back, cancel, resume after abandon; regional create end to end with the mock reaching a pre-filled form; "I understood this as…" + correction; four states (loading/empty/error/populated) incl. tenant with no customers and tenant with no AI usage; role without permission = clear refusal. Record each in QA_GUIDE self-run log with method "browser". Failures: fix and re-run; never soften expected results.
## Part 3 handover
QA_GUIDE (non-speaker can test multilingual; English flows; escape; skip-ahead; every permission case; clickable steps, no code). STATUS.md final table (+ "Verified how": browser / mocked / pending live key; no partial/pending/unknown). Known limits in plain English in both docs (streamed replies English; only sales invoice; Sarvam mock-verified; placeholder format unconfirmed; phrases need native review; Azure cost cap; pre-existing 500). Merge notes in STATUS.md: branch state, vitest.config.ts the one shared-config edit, env vars (SARVAM_API_KEY, base URL, SARVAM_COST_PER_1K_CHARS_USD NOT blank), seed script, order relative to global/admin and ai/workflows.
## Part 4 live check
LIVE_VERIFICATION.md opens with placeholder check as step one + what failure looks like + fix is one constant; then phrases, regional create, uncached latency. Top line: if any step fails the system degrades rather than misleads — nothing can produce a wrong record, only a worse translation.
## Part 5 close
Full suite green vs BASELINE.md command; tsc, eslint, next build clean; clean tree; fresh-worktree build+test; final report (decisions, browser results, status table, anything open).
## Rules unchanged
Azure stays brain · additive only (vitest.config.ts the single declared exception) · English path must not regress · never autocorrect input · protected entities never altered · fail open · never act on a guess for financial · every provider call metered · no secrets · Sarvam mocked in tests · suite green vs baseline · never push/merge. Ask only if browser pass shows a whole surface doesn't work.
