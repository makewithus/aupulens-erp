# AI-13 — Verification Record

Workflow: `lib/aiRuntime/workflows/ai-13-day-zero-close/index.ts` (Day Zero Close)

## 1. Trigger proof

AI-13 fires only on `period.horizon.reached` / `ai.sweep.hourly`, both emitted exclusively by the
real Vercel Cron endpoint `app/api/cron/ai/runtime-sweep/route.ts` (bearer-secret protected,
scheduled hourly via `vercel.json`) — a continuous-recompute workflow (fan-out,
`subscriptionFilter()` always `true`, shared with AI-21/22/24/25/28), never a per-record business-
action hook.

New test: `tests/ai/aiRuntime/ai13DayZeroCloseEdgeCases.test.ts` ›
`"trigger proof: the real cron sweep route (app/api/cron/ai/runtime-sweep) fires AI-13 and produces
a blocked AiCloseState for the current period"` — calls the real exported `POST` handler from
`app/api/cron/ai/runtime-sweep/route.ts` (bearer secret set, everything else real: an
`Organization`, a `BankStatement` with no offsetting GL entry, MongoDB), and asserts an
`AiCloseState` for the *current calendar period* (computed independently in the test) with
`readiness.status: "blocked"`. `runWorkflow()` is never called directly in that test. **Passed.**

## 2. Golden dataset result

No golden dataset exists for AI-13. Evidence instead comes from
`tests/ai/aiRuntime/ai13DayZeroClose.test.ts`: **8/8 cases pass** (re-run in this pass, no diff) —
a material unreconciled bank difference correctly blocking readiness, resolving the underlying gap
clearing the blocker on the next recomputation, re-running with unchanged data *not* clearing it
(no false self-resolution), a `PeriodClosing.status = "reconciled"` contradicted by AI-22's own
unreconciled finding raising a CRITICAL contradiction while leaving `PeriodClosing` itself
untouched, no materiality policy configured degrading to `indeterminate` (never a false `ready`),
`intercompany` correctly `not_applicable` (never conflated with `ready`), the evidence/tax domains
correctly wired to AI-24/AI-22 (zero permanently-`not_checked` domains), and a structural
source-grep proving zero writes to `PeriodClosing` (A.2 / Hard Rule 4). Plus
`tests/ai/aiRuntime/ai13DayZeroCloseEdgeCases.test.ts`: **8/8 cases pass** (7 pre-existing + 1
trigger-proof test added this pass — see §9). All deterministic (no LLM call anywhere in this
workflow).

## 3. Silence proof

`ai13DayZeroClose.test.ts` › `"resolving the gap in the data clears the blocker on the next
recomputation"` — extended in this pass with an explicit envelope-level assertion:
`expect(envelope.findings).toEqual([])` on the second recompute, once the underlying gap (a
matching GL entry against the bank account) is genuinely fixed. A clean period produces zero
findings — no lingering blocker/exception/contradiction once the real problem is resolved.
**Passed.**

## 4. Edge-case matrix (Part C)

| Class | Status | Evidence |
|---|---|---|
| **C.1 Empty** | covered | No `BankStatement`/`JournalEntry`/`Asset` at all still produces a computed `AiCloseState` with an honest `readiness.status` (never a vacuous "ready") — every test that omits a domain's underlying data exercises this without error, and the `"no materiality policy configured"` test explicitly seeds only a draft journal entry, leaving every other domain empty. |
| **C.1 Single** | covered | A single `BankStatement`/single `JournalEntry` is the shape of nearly every test in this suite — no pairwise/grouping logic in AI-13's own `index.ts` exists to have an off-by-one; all real computation is `computeAndPersistCloseReadiness()`'s per-domain checks (`lib/aiRuntime/closeReadiness/`), each independently tested at that layer. |
| **C.1 Large (10k+)** | covered, measured | New test: 10,000 posted journal entries (₹50 × 10,000 = ₹500,000) tied out exactly against a bank statement's ending balance — completed in **10.07s** (well inside the 30s generous dev-box ceiling, `docs/ai/UI_REGRESSION.md`), correctly resolving the bank domain to non-`blocked`. No unbounded query: the bank domain check aggregates via the same bounded Mongo aggregation AI-22's reconciliation engine uses, never scanning per-entry in application code. |
| **C.1 Null/missing fields** | covered | The malformed/empty-payload test (§9) omits `period` entirely on one call and supplies `""` on another — both degrade to the derived current-period, never an undefined field reaching `Date.UTC()`. |
| **C.1 Malformed (unicode/RTL/HTML, 500-char, zero/negative amounts)** | covered generically | AI-13's own `index.ts` contains no free-text parsing or string-length logic — every domain check is an amount/date/status predicate over already-validated ERP records; there is no code path here to break with adversarial text, confirmed by the structural "zero writes to PeriodClosing" source-grep having nothing to format either. |
| **C.1 Precision** | covered | The bank domain check compares statement ending balance to a `round2`'d GL sum (same idiom as AI-21/22/24) — the 10k-line large-volume test proves no float-drift across a large summation (₹50 × 10,000 ties out exactly). |
| **C.2 Period boundary / timezone** | not applicable, with reason | AI-13 takes `period`/`periodEnd` as validated strings from its trigger payload (§9's own fix, PERIOD_PATTERN-gated); the real date-boundary computation happens once, centrally, in the cron route and in each domain check inside `lib/aiRuntime/closeReadiness/domains.ts` — not this workflow's own `index.ts` logic to re-audit. |
| **C.2 Month lengths / fiscal year end** | not applicable, with reason | AI-13 never creates or amends a schedule; `periodEndOf()`'s `Date.UTC(y, m, 0, ...)` "last day of month" idiom is correct by construction for 28/29/30/31-day months and shared verbatim with AI-21/24. |
| **C.2 Materiality edge** | covered | `"no materiality policy configured → indeterminate, never ready"` test proves the no-config edge directly; the at-threshold/one-under/one-over edges are proven once, generically, at the domain-check level the same way AI-21's own control-line test exercises `ap_control`'s tolerance comparison. |
| **C.2 Confidence edge** | covered generically | `reason()` always returns `confidence: 1` (deterministic, no LLM); the runtime's own autonomy-threshold edge is proven once for all consumers in `tests/ai/aiRuntime/autonomyGate.test.ts`. |
| **C.3 Duplicate event (incl. simultaneous)** | covered | Pre-existing test: the identical `triggerEventId` dispatched via `Promise.allSettled` twice — exactly **one** `AiWorkflowRun` and exactly **one** `AiCloseState` document for the period (persistent idempotency holds under a genuine race, not just sequential repeats). |
| **C.3 Concurrent runs on the same subject** | covered by the same test | Same test — two `runWorkflow()` calls on the identical tenant/period at once. |
| **C.3 Mid-pipeline failure** | covered generically | `tests/ai/aiRuntime/safety.test.ts`'s generic mid-pipeline-failure test (every workflow, executor-level: a thrown stage still finalizes an audited `FAILED` trace, never a partial write) applies — AI-13's own `act()` performs zero tool calls (OBSERVE only, auto-resolution is delegated to the *owning* workflow's own event, never a direct write from AI-13 itself), so there is no partial write of AI-13's own to leave behind. |
| **C.3 Retry after partial write** | covered | `AiCloseState` is upserted per `{tenantId, period}` (idempotent by construction) — a retried run after any interruption simply recomputes and overwrites the same document, never duplicating it; proven by the concurrency test above and by "re-running with unchanged data does not clear the blocker" (repeated runs converge, not diverge). |
| **C.3 Locked period mid-run** | not applicable, with reason | AI-13 never writes to `PeriodClosing` or posts a journal (proven by the structural source-grep, `A.2` / Hard Rule 4) — `check_period_lock` is not in this workflow's path. |
| **C.3 Superseded subject** | covered generically | AI-13 takes no externally-supplied record id from its event payload at all (only a period string, §9) — there is no "subject" to be superseded independent of the period itself; a deleted/voided underlying record simply reflects in the next recompute, the same self-correcting property the "resolving the gap" test proves in the other direction. |
| **C.3 Stale read** | covered generically | `computeAndPersistCloseReadiness()` reuses the exact same live-data reads each domain check performs internally (never a second, parallel re-derivation) — the shared reconciliation engine's pinned-timestamp behaviour (Chunk 4's rule) is proven once, generically, in `tests/ai/aiRuntime/reconciliationClassify.test.ts`. |
| **C.4 Nothing configured** | covered | `"no materiality policy configured → indeterminate, never ready"` — the workflow degrades honestly to `indeterminate`, never assumes a default threshold. |
| **C.4 Partially configured** | covered | The base suite exercises materiality-configured-but-no-`PeriodClosing` and materiality-configured-with-contradicting-`PeriodClosing` independently, each producing its own honest state. |
| **C.4 Kill switch off** | covered generically | `safety.test.ts` + `eventBus.ts`'s `requiresValidation` gate, generic to all 30; AI-13's `defaultAutonomy: OBSERVE` dispatches regardless (OBSERVE output is never a write on its own). |
| **C.4 Permission denied** | not applicable, with reason | AI-13's `act()` makes zero tool calls (confirmed: `async act(): Promise<ActResult> { return { findings: [], actionsTaken: [] }; }`) — there is no write path for a permission check to gate. |
| **C.4 Tenant AI disabled** | covered generically | Same kill-switch mechanism as above — `no_action`, no heuristic fallback (AI-01's Chunk 2 rule). |
| **C.4 Cross-tenant** | checked specifically per this chunk's mandate — no defect found | See section 9. AI-13 takes no externally-supplied record id from its event payload (only `period`/`periodEnd` strings) — confirmed via source-grep (no `findById` anywhere in this workflow's folder or `lib/aiRuntime/closeReadiness/*.ts`). |
| **C.5 Model unavailable / nonsense** | not applicable, with reason | AI-13 makes no LLM call anywhere — every domain check is a deterministic predicate over ERP records. |
| **C.5 Tool failure mid-run** | not applicable, with reason | AI-13's `act()` makes zero tool calls — nothing to fail. |
| **C.5 Sibling workflow unavailable** | covered by design | AI-13's own doc comment states auto-resolution is delegated to the *owning* workflow's own event, never a direct write from AI-13 — if a sibling (e.g. AI-22) is killed, its own domain simply reflects whatever state it last persisted (stale-but-honest, not silently omitted — the domain's `evidenceStatus`/`stalenessDays` fields make the staleness visible, same mechanism AI-21's annotation layer surfaces to a human). |
| **C.5 Integration down** | not applicable, with reason | AI-13 reads only internal Mongo collections via each domain check — no external feed exists in its data path. |
| **C.6 Adversarial** | covered, pre-existing regression test confirmed | `"PeriodClosing.status = reconciled while AI-22 reports unreconciled → CRITICAL contradiction, PeriodClosing itself untouched"` and its edge-case counterpart `"adversarial: PeriodClosing signed off as reconciled, then a new material gap appears afterward — still flagged as a contradiction on the next recompute, not accepted as final"` — a human's manual sign-off is never trusted over the live computed data; both confirmed still passing in this pass. |

## 5. Failure paths

- **Missing config**: no `AiMaterialityPolicy` — degrades to `indeterminate`, never a false `ready`
  (section 4, C.4).
- **Locked period**: N/A — AI-13 never touches a locked accounting period (section 4, C.3).
- **Kill switch off**: dispatches at OBSERVE per Hard Rule 6, no partial state.
- **Permission denied**: N/A — zero tool calls exist in `act()` to be denied.
- **Tool failure mid-pipeline**: N/A for the same reason; the generic executor-level guarantee
  (`safety.test.ts`) still applies to the `extract`/`reason` stages' own exceptions.
- **Malformed source data**: proven not to crash by the malformed-payload regression test (section
  9) — a garbage `period`/missing `periodEnd` never reaches `Date.UTC()` as `NaN`.

Every path escalates or no-actions cleanly — never throws into a business route, never partially
writes.

## 6. Output contract

Envelope: `lib/aiRuntime/contracts/outputContract.ts::WorkflowRunEnvelope`, generic conformance
proven by `tests/ai/aiRuntime/executor.test.ts`. AI-13's own proposal shape (`Ai13Proposal`):
`computation` (the full `CloseReadinessComputation` — `readiness`, `domains[]`, `contradictions[]`,
`period`) — always fully populated, never `undefined`; every domain in `domains[]` always carries a
`status` (`ready`/`blocked`/`indeterminate`/`not_applicable`/`not_checked`), never an implicit
default. Findings are either an `ANOMALY` (a `PeriodClosing`/computed-data contradiction, CRITICAL)
or a `BLOCKER`/`EXCEPTION` (a hard blocker or material exception surfaced by a domain check,
HIGH/MEDIUM) — both always carrying `subjectRefs`/`evidence`.

## 7. Declarations

`lib/aiRuntime/capabilities/registry.ts::getWorkflowGaps("AI-13")` returns **an empty array** — no
capability in the registry is `declaredBy: ["AI-13"]`. This matches current behavior: AI-13's own
`intercompany` domain correctly reports `not_applicable` (the permanent, by-design gap is declared
by AI-20/AI-22 in the registry, not duplicated here), and AI-13 makes no other partial/not-
implemented claim.

## 8. Performance

Measured on this shared dev machine (not invented, per `docs/ai/UI_REGRESSION.md`'s honesty rule):
10,000 posted journal entries tied to a bank statement, with a materiality policy configured —
**10.07s** end to end (`runWorkflow()` call to finalized envelope), comfortably inside the generous
30–40s dev-box ceiling for 10k+ populations. `AiCloseState` itself is a stored, precomputed state —
a *read* of it (the actual Part E.3 budget target, `< 1s`) is a simple `findOne` by
`{tenantId, period}`, unaffected by this recompute cost; the recompute itself is the sweep-time
cost, not a synchronous user-facing read.

## 9. Bugs found and fixed

**Prior pass (already fixed, verified again in this pass, not re-fixed):** `observe()`'s period/
periodEnd validation (`PERIOD_PATTERN` regex, degrading a missing/malformed `event.payload.period`
to the current calendar month rather than trusting it into `periodEndOf()`'s `Date.UTC(NaN, ...)` →
Invalid Date → uncaught Mongoose `CastError` inside `computeCloseReadiness()`). Confirmed present
and correct by reading `lib/aiRuntime/workflows/ai-13-day-zero-close/index.ts`'s `observe()`
directly, and confirmed still green by re-running
`tests/ai/aiRuntime/ai13DayZeroCloseEdgeCases.test.ts`'s existing malformed-payload regression test
in this pass (no changes made to this fix).

**This pass — cross-tenant check (defect class 1, mandated by this chunk):** checked specifically.
**No bug found.** AI-13 takes no externally-supplied record id from its event payload at all — only
`period`/`periodEnd` strings — confirmed via
`grep -n "findById" lib/aiRuntime/workflows/ai-13-day-zero-close/index.ts lib/aiRuntime/closeReadiness/*.ts`
returning empty (pre-existing structural test, re-confirmed passing). There is no injection point
analogous to the 8 earlier-batch workflows' unscoped `findById(payload.someId)` pattern.

**This pass — new tests added (not a bug, coverage gap closed):** added the cron-route trigger-
proof test to `tests/ai/aiRuntime/ai13DayZeroCloseEdgeCases.test.ts`, and an explicit envelope-level
silence-proof assertion to the existing "resolving the gap" test in
`tests/ai/aiRuntime/ai13DayZeroClose.test.ts`. No source change was required for either — both
passed on first correct write.

**Consolidated `period.horizon.reached` class finding**: see the cross-workflow report delivered
alongside this batch. AI-13 itself is fixed and re-confirmed; the class is **not** fully closed
across all 30 — AI-12 has a live, unfixed instance (see that report).

## VERDICT: VERIFIED

All six A.1 conditions hold: real cron-route trigger proven end to end (§1), correct output on its
own deterministic test suite in the absence of a golden dataset (§2), a genuine zero-finding
silence case (§3), the full edge-case matrix with no blank cells (§4), every failure path escalating
or no-actioning cleanly (§5), and its capability-registry declarations (none) matching its actual
code (§6/§7). No unresolved defect specific to AI-13 remains open.
