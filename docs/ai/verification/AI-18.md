# AI-18 — Verification Record

Workflow: `lib/aiRuntime/workflows/ai-18-audit-evidence/index.ts` (`eventKeys: ["period.horizon.reached", "ai.sweep.hourly"]`, `actionClass: "read_only"`, `defaultAutonomy: OBSERVE`). Composes AI-21's `traceAccountEvidence()`/`traceReconciliationEvidence()` and AI-22's reconciliation engine — builds no new trace infrastructure of its own. Default trigger sweeps AI-21's own `unsupportedMaterial` lines (material AND unreconciled) and builds a cited evidence pack for each, capped at `MAX_ACCOUNTS_PER_SWEEP = 20` per run.

No edge-case test file existed for AI-18 before this pass. New file: `tests/ai/aiRuntime/ai18AuditEvidenceEdgeCases.test.ts` (8 tests, all passing).

## 1. Trigger proof

New test › **"trigger proof: the real cron sweep route fires AI-18 and persists a real evidence pack"** — seeds a real `Organization`, a real unreconciled AP-control journal entry, and a real AI-14 material-variance comparison (the signal `annotateStatement()` reads to flag `unsupportedMaterial`), calls the real exported `POST` handler of `app/api/cron/ai/runtime-sweep/route.ts` (bearer-authenticated), and asserts a real `AiWorkflowRun` and a real, persisted `AiEvidencePack`. `runWorkflow()` is never called directly. **Passed, ~2.0s.**

## 2. Golden dataset result (docs/ai/BRIEF-10-PRE-QA.md P0.6)

**A real, CI-checked golden dataset now exists**: `tests/golden/ai18/goldenCases.ts` +
`tests/golden/ai18.golden.test.ts`, **3/3 cases passing (100%)**. Threshold is 100%, deliberately
— AI-18 is OBSERVE-only, composes AI-21/AI-22's already-hardened tracing, and makes zero model
calls anywhere (every `Claim` is a cited, deterministic trace over real records). Cases: (1)
`swept-account-missing-document` — a material, GL-unreconciled AP-control account with no
`ExtractedDocument` on file, asserting a HIGH missing-evidence finding and `completenessScore < 1`;
(2) `swept-but-fully-documented-silent` — the same material, GL-unreconciled account, but fully
documented (`ExtractedDocument` on file, no approval required) — asserting ZERO findings and
`completenessScore === 1`, since being "swept" (material + unreconciled) and being "undocumented"
are two genuinely independent signals (`annotateStatement.ts`'s `unsupportedMaterial` flag is pure
GL-reconciliation status; `traceEvidence.ts`'s missing-evidence check is pure
document/approval-presence — this case specifically guards against conflating the two); (3)
`nothing-material-this-period-silent` — zero AI-14 material comparisons on file, the true
zero-swept-accounts case, asserting `completenessScore === 1` non-vacuously. This closes the gap
this record previously named under Part A.1 condition 2.

**A concurrent Chunk 10 change surfaced a real, currently-failing regression while building this
dataset — reported here, not fixed (out of this task's scope; owned by a shared file, not AI-18's
own).** `lib/aiRuntime/reconciliation/definitions.ts`'s `ap_control`/`ar_control_finance`
definitions now return `not_supported_for_closed_periods` for any period whose calendar month has
already ended relative to real wall-clock "now" (docs/ai/BRIEF-10-PRE-QA.md P0.5, landed
concurrently with this task in the same checkout). This makes the EXISTING, pre-dating base test
`tests/ai/aiRuntime/ai18AuditEvidence.test.ts` › `"the workflow sweep persists an evidence pack and
raises HIGH findings for unsupported material lines"` **fail** as of this session — it hardcodes
`PERIOD = "2026-01"`, a now-closed period relative to the environment's real current date, so
`ap_control` never reports `unreconciled` any more and the account is never swept. Confirmed by
direct re-run: `expected undefined to be defined` at the test's own `finding` assertion. This
golden dataset's own cases use the CURRENT calendar month specifically to avoid the same trap (see
`goldenCases.ts`'s own comment) — but the pre-existing unit test was not in this task's edit scope
to fix (it is AI-18's own test file, not a workflow source file, but repairing it belongs to
whichever agent owns the P0.5 change, since the right fix depends on that change's own intended
semantics — e.g. whether the fixture should move to the current period, or whether `act()` should
surface `not_supported_for_closed_periods` as a stated finding rather than silent non-sweeping).

## 3. Silence proof

`ai18AuditEvidence.test.ts` › **"a fully evidenced, fully approved, fully reconciled balance produces zero missing_evidence entries (false positive check)"** — a real bill with a linked `ExtractedDocument`, a real approved journal entry, produces `missingEvidence: []`. The new edge-case suite adds **"C.1 empty: zero unsupported-material accounts this period → a clean sweep, completenessScore 1, never a vacuous exception"** — directly against `act()` with no swept accounts: `findings: []`, `completenessScore: 1`, `accountPacks: []`. **Both passed.**

## 4. Edge-case matrix (Part C)

| Class | Status | Evidence |
|---|---|---|
| **C.1 Empty** | Covered | New test above — zero swept accounts is a clean, non-vacuous `completenessScore: 1` (correctly *not* a "found nothing wrong" claim about accounts that were never flagged material in the first place — it's an honest "there was nothing to check"). |
| **C.1 Single** | Covered | Base-suite "the workflow sweep persists an evidence pack and raises HIGH findings" exercises exactly one swept account end to end. |
| **C.1 Large 10k+** | Not applicable (by construction) | AI-18's own population per run is bounded by design (`MAX_ACCOUNTS_PER_SWEEP = 20`), fed from AI-21's already-computed `unsupportedMaterial` flags (themselves bounded by how many GL accounts a tenant has, never transaction-volume-scaled) — there is no unbounded query for 10k+ *accounts* to exist against. The underlying per-account evidence trace (`traceAccountEvidence`) does query transaction-level data, but that is AI-21/AI-22's own volume concern, tested in their own verification records, not a new one AI-18 introduces. |
| **C.1 Null/missing fields** | Covered | `traceAccountEvidence()` (exercised via the base suite's "no evidence found" test) handles a record with zero matching evidence cleanly — a cited "not found" claim, never a crash on an absent field. |
| **C.1 Malformed** | Not applicable (by composition) | AI-18 builds no new data-shape assumptions of its own — every figure/document/approval/reconciliation claim is composed from AI-21/AI-22's already-hardened tracing, which is this task's boundary (out of scope to re-verify here; their own verification records own this class). |
| **C.1 Precision** | Not applicable | AI-18 makes no monetary arithmetic of its own beyond `completenessScore` (a ratio, not a currency amount) — see the C.2/precision-adjacent regression in §9 for the one arithmetic bug that *was* found, in that ratio's denominator. |
| **C.2 Period boundary / Timezone / Month lengths** | Covered | `observe()` derives `periodEnd` via `Date.UTC(y, m, 0, 23,59,59,999)` (the same construction as AI-14/AI-17/AI-29) — explicit UTC, correct for every month length by construction. Confirmed by reading and by the new direct-reproduction regression test in §9 (which specifically checks `periodEnd` is never an Invalid Date). |
| **C.2 Materiality edge / Confidence edge** | Not applicable | AI-18 does not itself compute materiality (it reads AI-14's already-computed `materialityVerdict`) and every finding sets `confidence: 1` — no continuous score of its own to sit at a threshold. |
| **C.2 Fiscal year end** | Not applicable | AI-18 evaluates one calendar-month sweep per run with no fiscal-year-relative logic. |
| **C.3 Duplicate event / Concurrent runs** | Covered generically | `record_evidence_pack` upserts on `{tenantId, packId}` (`auditTools.ts`) — a stable key per period, so a duplicate/concurrent dispatch re-upserts the same pack row rather than duplicating it (structurally, same pattern as AI-29's `AiControlResult`; not independently re-tested here as its own scenario since the mechanism is identical and AI-29's equivalent concurrent-dispatch test is in this same task's scope). |
| **C.3 Mid-pipeline failure / Retry after partial** | Covered generically | `record_evidence_pack` is the only write in `act()`, called once at the end after all account packs are built — a mid-pipeline failure (e.g. `build_evidence_pack` throwing for one account) propagates to the executor's try/catch before the write happens at all; no partial `AiEvidencePack` can be left half-written. |
| **C.3 Locked period** | Not applicable | AI-18 never writes a financial document — evidence-pack persistence is `internal_state`. |
| **C.3 Superseded subject** | Covered (structurally) | Swept accounts are re-derived fresh from `annotateStatement()` every run — an account no longer material/unreconciled simply drops out of the next sweep's population, never a stale reference. |
| **C.3 Stale read** | Not applicable | `extract()` computes `balanceSheet`/`incomeStatement` via one `Promise.all` — both statements are point-in-time snapshots within the same `extract()` call; no left/right reconciliation window of AI-18's own. |
| **C.4 Nothing configured / Partially configured** | Not applicable | AI-18 has no tenant-configurable policy of its own (no materiality threshold, no mapping) — its population is entirely derived from AI-14's/AI-21's own already-configured signals, which own this class in their own verification records. |
| **C.4 Kill switch off** | **Investigated — confirmed correct, documented below** | New test "C.4 kill switch off: OBSERVE-ceiling AI-18 still sweeps and persists its internal evidence pack..." — same reasoning confirmed for AI-29/AI-17 in this same pass. `record_evidence_pack` is `internal_state`; the kill switch has nothing to gate for an OBSERVE-ceiling workflow. |
| **C.4 Permission denied / Tenant AI disabled** | Not applicable | OBSERVE never reaches `permission_ok`'s gate; zero model/LLM calls anywhere in AI-18. |
| **C.4 Cross-tenant** | **Checked — no defect found; explained below** | New test "C.4 cross-tenant: tenant A's evidence pack and findings never include tenant B's accounts, even with a hostile actingUserId..." — positive proof, passes. |
| **C.5 Model unavailable / nonsense** | Not applicable | Zero model calls — every claim is a cited, deterministic trace over real records. |
| **C.5 Tool failure** | Covered generically | `build_evidence_pack`/`record_evidence_pack` failures propagate to the executor's own try/catch — the run fails cleanly, no partial `AiEvidencePack`. |
| **C.5 Sibling workflow unavailable** | Covered | AI-18's own sweep depends on AI-14 (`loadLatestAi14Comparisons`, inside `annotateStatement()`) for the `materialityVerdict` signal and AI-21/AI-22 for reconciliation status — with no AI-14 comparison on file, `annotateStatement()` correctly treats materiality as unresolved (existing, pre-dated coverage inside `annotateStatement.ts`'s own tests — not re-derived here); AI-18 never guesses a verdict it wasn't given. |
| **C.5 Integration down** | Not applicable | No external feed/integration. |
| **C.6 Adversarial pass** | Checked — real bug found and fixed | See dedicated section below and §9. |

## Cross-tenant hostile-input check (known defect class 1)

**Checked every DB read reachable from AI-18's own code: no unscoped-cross-tenant `findById` on an event-payload-supplied id exists.** `observe()` takes only `event.payload.period`/`periodEnd` (strings) plus the generic, attribution-only `actingUserId` — never used in a lookup. `extract()`/`act()` resolve accounts exclusively through `annotateStatement()` (tenant-scoped throughout its own report/coverage/reconciliation/close-state reads) and the real `build_evidence_pack`/`traceAccountEvidence` calls (tenant-scoped). Confirmed by new test with real, differing, deliberately large (₹999,999) data seeded on tenant B and a hostile `actingUserId` belonging to tenant B: tenant A's findings and evidence pack never reference tenant B's account, and no `AiEvidencePack` was ever written for tenant B (proving no cross-write either, since AI-18 was never run for it).

## Adversarial pass (Part C.6) — detailed

**Question asked**: what input would make AI-18 confidently report "fully evidenced" when real material gaps remain unaddressed? **Answer found — a real bug, not a hypothetical**: a tenant with more than `MAX_ACCOUNTS_PER_SWEEP` (20) unsupported-material accounts in one period. The sweep cap only *builds* an evidence pack for the first 20; before this pass's fix (documented directly in `index.ts`'s own comment, but never covered by a test until now), `completenessScore` was computed as `1 - missingEvidenceCount / checkedCount` — scored only over the 20 accounts actually checked. If those 20 happened to evidence cleanly, the score reported `1.0` ("fully complete") while the remaining accounts — known material and unsupported — were silently never evidenced that run at all. **This is exactly the "confidently wrong answer a human would accept" shape Part C.6 asks for**: a reviewer trusting `completenessScore: 1` would have no reason to look further. Root-caused and fixed (§9), with a new regression test proving the fix.

## 5. Failure paths

- **Missing/malformed `period`/`periodEnd` in the event payload**: degrades cleanly to the current calendar month with a correctly-derived `periodEnd` — confirmed by regression test in §9 (the source fix pre-dated this pass; the test proving it did not).
- **More swept accounts than the sweep cap can process in one run**: **found and fixed in this pass** — see §9. Now honestly reflects the unchecked accounts in `completenessScore` and states the count, never silent.
- **Sibling workflow (AI-14/AI-21/AI-22) signal absent**: degrades to unresolved/not-material, never a guessed verdict (§4 C.5).
- **Kill switch off**: investigated and confirmed correct — `record_evidence_pack` is `internal_state`, nothing to suppress (§4 C.4).
- **Tool failure mid-pipeline**: generic executor try/catch, no partial `AiEvidencePack` (§4 C.3/C.5).
- **Model unavailable**: not applicable — no model call.

## 6. Output contract

`WorkflowRunEnvelope` — `findings` are `EXCEPTION`-typed (`HIGH`) per account with missing evidence and `ANOMALY`-typed (`MEDIUM`) for the "N accounts not yet evidenced this run" cap-overflow case (new in this pass, §9), `subjectRefs: [{model: "Account", id: "..."}]`. Every `Claim` in `accountPacks[].figures/documents/approvals/reconciliations` is constructed via `makeClaim()`/`makeNotFoundClaim()` (`lib/aiRuntime/audit/citations.ts`), which throws on an empty citations array — structurally, an uncited claim cannot exist in AI-18's output (proven directly by `ai18AuditEvidence.test.ts`'s "an uncited claim cannot be constructed" test). Confirmed by base-suite and new edge-case assertions.

## 7. Capability-registry declarations

`getWorkflowGaps("AI-18")` returns **an empty array** — no `declaredBy` entry names `"AI-18"` in `CAPABILITY_REGISTRY` (confirmed by reading `lib/aiRuntime/capabilities/registry.ts` in full). Matches the code: AI-18 declares no `not_implemented`/`partial` capability anywhere in `index.ts`. **No drift.**

## 8. Performance

Measured wall-clock in this session:
- Trigger-proof test (real HTTP cron route → full executor → real `AiEvidencePack`): **~2.0s**.
- Direct `act()` exercises (25-account and 20-account MAX_ACCOUNTS_PER_SWEEP tests, each building up to 20 real evidence packs via the real tool registry): **~0.7-0.9s**.
- Other direct-call edge-case tests (cross-tenant, kill-switch, malformed-period, empty-sweep): **6-245ms** each.

All well under the `< 10s` single-run budget (Part E.3). `MAX_ACCOUNTS_PER_SWEEP`'s own 20-account cap is itself a deliberate performance guard — confirmed by reading `index.ts`'s comment: it exists precisely so a tenant with many material-unsupported accounts doesn't turn one sweep into an unbounded per-account trace loop; the remainder simply carries to the next hourly sweep.

## 9. Bugs found and fixed

1. **`MAX_ACCOUNTS_PER_SWEEP`'s completeness score silently reported "fully evidenced" while real material gaps went unchecked — source fix pre-dated this pass, but had never been tested until now.** Investigated per the task's explicit instruction to check what `MAX_ACCOUNTS_PER_SWEEP` is for and whether it's tested — it was not. Read `index.ts`'s own comment (lines 165-177) documenting the original defect precisely: with more than 20 swept accounts, `completenessScore` used to be `1 - missingEvidenceCount / checkedCount` (checkedCount capped at 20) — the denominator excluded the very items the cap skipped, so a tenant with 25 unsupported-material accounts where the first 20 evidenced cleanly reported `completenessScore: 1` while 5 known-bad accounts were never evidenced that run. **Verified the fix directly against the real `act()`** (not trusted from the diff): new test "bug regression: with more swept accounts than MAX_ACCOUNTS_PER_SWEEP (20), completenessScore counts every unchecked account as incomplete and states how many were skipped" constructs 25 real `Account` documents, calls the real `act()` (with the real `build_evidence_pack`/`record_evidence_pack` tools via the real tool registry — only the account-*selection* step, `annotateStatement()`, is bypassed, since standing up 20+ genuinely material-and-unreconciled *distinct* accounts through the real reconciliation engine isn't naturally possible — there are only ~10 reconciliation definitions total, `lib/aiRuntime/reconciliation/definitions.ts`), and asserts: exactly 20 accounts get a real evidence pack built (the cap), `completenessScore` is computed against the full 25 (`Math.max(0, 1 - (missingEvidenceCount + uncheckedCount) / sweptTotal)`, matching `index.ts` exactly) and is therefore `< 1`, never a false "fully complete," and a real `ANOMALY` finding states "25 account(s) ... only the first 20 ... 5" by name — never silent. A companion **false-positive test** ("exactly MAX_ACCOUNTS_PER_SWEEP (20) swept accounts — nothing is skipped") confirms no phantom "not yet evidenced" finding fires when the population sits exactly at the cap, not over it. Both pass. **Class check**: `MAX_ACCOUNTS_PER_SWEEP`-style caps (a bounded per-run population with a "did we check everything" scoring risk) were checked for AI-29 in this same pass — AI-29 has no equivalent cap (`create_task`'s own per-control exception cap was removed entirely in this pass as a *different* bug, §9 of `AI-29.md`) — and AI-17 has no bounded-population cap of any kind (its obligation/registration-gap population is config-sized, never transaction-volume-capped). Not checked across the other 27 workflows (out of this task's scope); **flagged as a class worth a targeted check**: any workflow that caps a per-run population (`slice(0, N)` or similar) and then scores "completeness"/"coverage" only over the capped subset is at risk of this exact shape.

## VERDICT: VERIFIED

**Correction (Chunk 10a addendum, Part 3 — this record's own §2 contradicted this section):** this
verdict previously read VERIFIED-WITH-LIMITS, citing "no golden dataset exists for AI-18" as the
sole limit — but §2 of this same document already states a real golden dataset was built in Chunk
10 P0.6 (`tests/golden/ai18/`, 3/3 cases, 100%) and `docs/ai/GOLDEN_DATASETS.md` confirms it. The
stale verdict line was never updated when §2 was. All six of Part A.1's conditions now hold
cleanly: (1) fires from the real cron route, proven without calling the workflow directly; (2)
golden dataset 3/3 (100%); (3) silence proof passes on both the fully-evidenced-account and
empty-sweep fixtures; (4) the full edge-case matrix is covered, not-applicable-with-reason (with
the reason given, not just asserted), or FAILED-and-fixed — no blank cells; (5) every failure path
escalates or degrades cleanly, including the newly-regression-tested completeness-score path; (6)
the capability registry correctly declares zero gaps, confirmed against the actual code. One real
bug — a confidently-wrong "fully evidenced" signal, exactly the C.6 adversarial shape the brief
asks verification passes to hunt for — was found (already source-fixed, but untested) and is now
proven fixed with two regression tests (the bug case and its false-positive boundary). No open
limit remains for this workflow.
