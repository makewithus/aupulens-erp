# CAPABILITY_MAP.md — Phase 0 Discovery

> One row per workflow AI-01…AI-30. Built from `SYSTEM_INVENTORY.md` plus a targeted deep-dive
> (see `docs/ai/IMPLEMENTATION_LOG.md` Chunk 1 entry for the research agent's full findings).
>
> **Spec availability**: full business-meaning specs have been delivered for all 30 workflows,
> AI-01 through AI-30, as of Chunk 8a (2026-09-04) — **all 30 are now BUILT**. AI-19/AI-11/AI-27/
> AI-26/AI-30 (the final five, Batch G) shipped in Chunk 8a; see `docs/ai/IMPLEMENTATION_LOG.md`'s
> Chunk 8a entry for the full report-back.
>
> **Chunk 8b (Final, 2026-09-04)** closed the catalogue: the natural-language control layer
> (AI-NL, `lib/aiRuntime/nl/**`, extends the existing Command Center — no 31st workflow, a
> cross-cutting control surface over all 30), nightly metrics (`AiMetricSnapshot`), a golden
> dataset for AI-27, governed learning-loop promotion, and the project's acceptance docs
> (`docs/ai/PRODUCT_TEST.md`, `AUTONOMY_RUNBOOK.md`, `README.md`). See
> `docs/ai/IMPLEMENTATION_LOG.md`'s Chunk 8b entry for the full report-back.
> For the remaining un-specced rows, the "Workflow" column uses a name inferred only from direct
> cross-references already present in Chunk 1's text (Part 0.2's table, Part 2.8, the AI-NL
> intent map, Part 6 epics) — marked `[inferred]` — never an invented specification. Per Chunk
> 1's own instruction, un-specced rows record only what the codebase already has that such a
> workflow would plausibly draw on — not a capability verdict against a spec we don't have.

| ID | Workflow | Status | Existing files | Existing models | Existing endpoints | Existing UI | What's missing |
|---|---|---|---|---|---|---|---|
| AI-01 | Document ingestion & accounting extraction | **BUILT (Chunk 2)** | Extended: `lib/docIntel/billCreate.ts` (additive `bill.created` emit), `extractionSchemas.ts` (additive `receipt` type for AI-04). New: `lib/aiRuntime/workflows/ai-01-document-ingestion/index.ts` | Extended: `models/ai/ExtractedDocument.ts` (+`fileHash`) | Existing routes unchanged; new event trigger via `safeEmitEvent` in `app/api/document-intelligence/extract/route.ts` | `app/document-intelligence/` (unchanged, still works byte-identically) | Event-triggered (`document.received`) DRAFT-autonomy workflow: hashes the file, resolves the vendor against `Vendor` (never auto-creates), extends duplicate detection with hash+PO matching, arithmetic/currency/tax-mismatch escalation, drafts via `draft_bill` + `link_evidence`. 9 tests. Tax-rate "selection" is proposal metadata only (`Invoice.invoiceLines[].taxIds` found vestigial — OPEN_QUESTIONS). Other `DOC_INTEL_TYPE`s still out of scope. |
| AI-02 | Ledger classification | **BUILT (Chunk 2)** | New: `lib/aiRuntime/workflows/ai-02-ledger-classification/{index.ts,bankingRuleEngine.ts}` — the first-ever interpreter for `BankingRule.criteria`/`criteriaMatch` | `models/finance/BankingRule.ts` (now applied, not just CRUD) | none new | Banking Rules settings UI (unchanged) | Explicit-rule-first (zero LLM calls, tested), then vendor/category history, then model ranking constrained to a pre-filtered active/postable/non-control candidate set (structurally cannot select a control account). `EXECUTE` only for draft records with a real acting user + validated `AiWorkflowPolicy`. 8 tests. Learned-rule-proposal-after-corrections not built (needs a `user.corrected_ai_output` event source that doesn't exist yet — OPEN_QUESTIONS #11). |
| AI-03 | Bank reconciliation | **BUILT (Chunk 2)** | New matcher: `lib/aiRuntime/workflows/ai-03-bank-reconciliation/{index.ts,matcher.ts}` — separate from `lib/accounting/matching.ts` (different scope, per A.1) | `models/finance/BankReconciliation.ts` now actually populated with match data | `reconcile_transaction`/`draft_journal` tools reuse the real `assertTransactionNotLocked` call site from `app/api/finance/bank/reconcile/route.ts`'s own logic | `/finance/reconciliation`, `/finance/bank` (unchanged) | Pass 1 exact match against posted `JournalEntry` lines (via `BankStatement.header.journalId`) — the only `EXECUTE`-eligible path. Pass 2 fuzzy and Pass 3 fee/interest/transfer/`unknown_ar_side`/unknown always `RECOMMEND`-shaped regardless of the run's gate outcome, per A.5. 9 tests incl. false-positive (wrong tenant), locked-period refusal, no-double-reconcile. Reconciliation *position* is a simplified placeholder for AI-22 (OPEN_QUESTIONS #12), not a true bank-vs-GL balance. |
| AI-04 | Expense intelligence | **BUILT (Chunk 2)** | New: `lib/aiRuntime/workflows/ai-04-expense-intelligence/index.ts` — fully deterministic, zero LLM calls (account classification already covered by AI-02, which also subscribes to `expense.submitted`) | New: `models/ai/AiExpensePolicy.ts` (seeded empty; absent policy ⇒ zero invented violations — the batch's most-emphasized false-positive guard) | none new | `/finance/expenses` (unchanged) | Policy check (category limit, prohibited category) + duplicate-claim detection (same employee/amount/date window, independent of policy). Card-feed matching and missing-receipt-threshold checks explicitly **not built** — no card feed model and no receipt-attachment field exist anywhere to check against (OPEN_QUESTIONS #9). 6 tests, headlined by the "no policy configured → zero violations invented" case. |
| AI-05 | Receivables operations | **BUILT (Chunk 5)** | New: `lib/aiRuntime/workflows/ai-05-receivables-operations/index.ts`. Extends `lib/sales/paymentAllocation.ts` (through `draft_receipt_allocation`, never fabricates a Payment) and `lib/sales/reminderEngine.ts` (one additive dispute-skip guard, no second reminder path) | New: `models/ai/AiDispute.ts`, `models/ai/AiCommunicationDraft.ts` | New tools: `draft_receipt_allocation` (module `sales`), `draft_communication`/`open_dispute` (`internal_state`) | none new | `DRAFT` for allocation completion and communication drafting; sending stays `NEVER_AUTONOMOUS` — no `send_reminder` tool exists. Short-payment-vs-partial is a documented 80% heuristic. Predicted payment dates use each customer's own paid-invoice history, not invoice terms. Sales-vs-Finance payment-state divergence is reported, never repaired (A.2). `DunningRule` deliberately not reused (`GLOSSARY.md`). 9 tests. |
| AI-06 | Payables operations | **BUILT (Chunk 5)** | New: `lib/aiRuntime/workflows/ai-06-payables-operations/index.ts`. Extends `lib/accounting/matching.ts` via new pure `computeLineVariances()` export — `runPOMatching()` itself untouched, its own pre-existing test file still green unmodified | New: `models/ai/AiPaymentRunProposal.ts` — "no payment-run concept exists anywhere," so this is new, not a wrapper; no release tool exists for it anywhere | New tools: `draft_match_annotation` (module `finance`, enriches `discrepancyNotes` only on an already-`mismatch` bill), `record_payment_run_proposal` (`internal_state`) | none new | Structured "which leg, by how much" (quantity/price/receipt, 1% tolerance) replacing a terse boolean verdict. Duplicate check reuses `run_duplicate_scan` unchanged. Three `checks_not_implemented`: vendor-bank-change hold, cross-source duplicate search (both A.3), and `early_payment_discount` (found during this workflow — no payment-terms field exists anywhere, `OPEN_QUESTIONS.md` #25). Payment release impossible at any confidence (asserted directly — no such tool exists). 11 tests. |
| AI-07 | Accrual intelligence | **BUILT (Chunk 3)** | New: `lib/aiRuntime/workflows/ai-07-accrual-intelligence/index.ts` — GRNI (`PurchaseOrder.orderLines[].receivedQty > billedQty`) is deterministic, zero LLM calls, built and proven first per the brief | `AiSchedule` (`accrual_reversal` type, exactly one reversing entry next period), `AiLearningRecord` (accrual-accuracy tracking on the matching bill) | none new | Journal Templates UI (unchanged) | `DRAFT` for GRNI accruals below materiality; `RECOMMEND` everywhere else. Recurring-vendor pattern-matching (algorithm step 2) deliberately not built — no 12–24-period statistical baseline exists to build it on honestly; documented in OPEN_QUESTIONS.md. 6 tests. |
| AI-08 | Prepaid / deferred schedule intelligence | **BUILT (Chunk 3)** | New: `lib/aiRuntime/workflows/ai-08-prepaid-schedule/index.ts` — first-ever consumer of the new schedule engine, proves it | `AiSchedule` (`prepaid`/`deferred_revenue` types) | none new | none | Stated (explicit date-range) vs. inferred (keyword-only) service-period detection; only `stated` can ever reach `DRAFT` — `inferred` is always `RECOMMEND`, never auto-drafted, by construction. 7 tests incl. the one-month-rent false positive. |
| AI-09 | Revenue recognition intelligence | **BUILT (Chunk 3)** | New: `lib/aiRuntime/workflows/ai-09-revenue-recognition/index.ts` — reads `SaleOrder`/`SalesInvoice`/`Customer` freely (A.2's bounded exception to the Batch A Sales-read boundary), writes only Finance-side journals, proven by a source-grep structural test | `models/sales/SaleOrder.ts`'s `revenueRecognition{ recognizedAt, amount, method }` — **A.2 investigation answered**: `recognizedAt`/`recognizedBy` are set in exactly one place, `app/api/sales/sale-orders/[id]/route.ts`, on a human `q2cStatus` transition — not derived from an engine. `.amount`/`.method` are never written anywhere; vestigial, same class as `Invoice.invoiceLines[].taxIds` | `/api/cron/sales/subscriptions-billing` (unchanged; billing timing, not recognition accounting) | none dedicated | Four-way (contracted/billed/delivered/recognised) divergence classification: deferred revenue, unbilled/accrued revenue, revenue leakage (loud, customer-named), fulfilment gap — each an independent check. `DRAFT` always this batch; nothing auto-posts. Milestone basis never auto-anything (no milestone-tracking data exists to guess from). 7 tests. |
| AI-10 | Fixed asset intelligence | **BUILT (Chunk 3)** | Extended: `app/api/finance/assets/compute/route.ts` now calls the shared `lib/accounting/depreciation.ts::computeMonthlyDepreciation()` (safe, behaviour-preserving refactor — both paths guaranteed to agree by construction). New: `lib/aiRuntime/workflows/ai-10-fixed-asset/index.ts`, `lib/accounting/registerToGl.ts` (reusable register↔GL control-account tie-out, built explicitly for AI-22 to consume in Chunk 4) | `models/finance/Asset.ts`, `AiSchedule` (`depreciation` type) | `/api/finance/assets`, `/api/finance/assets/[id]`, `POST /api/finance/assets/compute` (unchanged behaviour) | `/finance/assets` (unchanged) | Capital-candidate detection is `RECOMMEND`-only (asset creation never auto-writes — "capital vs expense is judgement"); no capitalisation threshold configured → `threshold_configured: false`, never an invented figure. Depreciation schedule creation is `CONTROLLED_AUTONOMOUS` (mechanical once a human has posted the asset), posting is `DRAFT` unless `autoPostSchedules`. 7 tests incl. a seeded 1-unit register-to-GL difference. |
| AI-11 | Inventory / COGS intelligence | **BUILT (Chunk 8a)** | Answers the standing inventory-account question live (`lib/aiRuntime/inventory/accountMapping.ts`, reusing `lib/accounting/inventory.ts`'s own real posting-account resolution — code "1300" first, `asset_current`/`asset_non_current` fallback). Subledger-to-GL reuses AI-22's own `inventory` reconciliation definition directly (`RECONCILIATION_DEFINITIONS.find(d => d.id === "inventory")`), never a second computation. New, genuinely real weighted-average-cost computation (`computeWeightedAverageCost`) — confirmed nothing else in the codebase computes one. Negative stock (with causing movement sequence), count variances (valued at WAC, against a new `AiInventoryCount` human-entry model), valuation anomalies, slow-moving/expiry. Margin-by-item routes through AI-15's ratio/trend detector family (`product_margin_step_change`), not a new alert path — explicitly documented as an ESTIMATE (units × `standard_price` as a COGS proxy; no real COGS-on-fulfillment posting path exists anywhere). `RECOMMEND` autonomy. | `models/ai/{AiInventoryCount,AiInventoryFinding}.ts` | none (internal workflow only) | none | 7 tests incl. a seeded 0.01-unit subledger-to-GL difference and the mandatory false positive. Closes `OPEN_QUESTIONS.md` #21/#24 (the "which accounts constitute inventory" question AI-22/AI-25 were both waiting on). |
| AI-12 | Tax intelligence | **BUILT (Chunk 6)** | New: `lib/aiRuntime/workflows/ai-12-tax-intelligence/index.ts`, `lib/aiRuntime/tax/rebuildTaxProjection.ts`. Extends `lib/aiRuntime/reconciliation/definitions.ts` — the `tax` definition flips from Chunk 4's `not_implemented` placeholder to real, taking AI-13's permanent `not_checked` domains to zero | New: `models/ai/AiTaxTransaction.ts` (rebuildable projection, never a source of truth — A.1), `models/ai/AiComplianceProfile.ts` (human-entered, structurally read-only, A.2) | New tools: `get_tax_transactions`/`get_compliance_profile`/`run_tax_reconciliation`/`build_tax_workpaper` (read/analyse), `rebuild_tax_projection` (`internal_state`, the only write) | Policy tab (compliance profile form, Chunk 6) | RECOMMEND. Substantive work runs entirely inside `act()` — `extract()`/`reason()` never receive a tool handle, so the rebuild-then-reconcile sequence can't happen earlier in the pipeline; `reason()`'s placeholder proposal is mutated in place once `act()`'s real numbers land (executor.ts serializes `reasoned.proposal` only after `act()` returns). Three-way reconciliation (ledger/transactions/return) — transactions and return tie exactly by construction, the meaningful gap is always ledger-vs-either. `taxRateRef`/`taxType` always `null` (no source document reliably links to a `TaxRate` — same vestigial-field class as `Invoice.invoiceLines[].taxIds`). Jurisdiction resolved only for single-registration tenants, else `jurisdiction_unresolved`. Workpaper box set is deliberately universal, not a real per-jurisdiction filing layout (`OPEN_QUESTIONS.md` #28). Cannot mutate `TaxRate` at any confidence (asserted); no `submit_filing`/external-submission tool exists anywhere (asserted). 11 tests. |
| AI-13 | Day Zero Close (close readiness / blocker controller) | **BUILT (Chunk 4)** | New: `lib/aiRuntime/workflows/ai-13-day-zero-close/index.ts`, `lib/aiRuntime/closeReadiness/{classify.ts,domains.ts,compute.ts}`. Wraps AI-22's reconciliation engine + AI-07/08/09/10's own overdue/stale/exception signals — "most of AI-13 is aggregation of work already done," per spec | `models/ai/AiCloseState.ts` (new, persisted per `{tenantId, period}`); reads `PeriodClosing` (real close-checklist state machine) but never writes it | `/api/finance/period-closing` unchanged | `/finance/period-closing` unchanged | OBSERVE only. 15 close domains, each `ready\|blocked\|at_risk\|not_applicable\|not_checked` — tax/evidence always `not_checked` (no tax ledger; AI-24 not yet integrated into recompute), intercompany always `not_applicable`. Pure readiness classifier (`classify.ts`) unit-tested against a fixture matrix. A `PeriodClosing.status` contradicted by computed data raises a CRITICAL finding, never a status change. Revenue domain's AI-09 Sales-read half not replicated (boundary decision, `OPEN_QUESTIONS.md` #20). 7 tests. |
| AI-14 | Flux analysis | **BUILT (Chunk 5)** | New: `lib/aiRuntime/workflows/ai-14-flux-analysis/index.ts`. Extends `lib/accounting/reports.ts` — new pure `getAccountTransactionDetail()` export for line-level drill-down; `buildPostedJournalReport()` (account totals) untouched | none new | none new | none new | OBSERVE, read-only by construction (no write tool). Comparative basis is `Budget` where one exists for the period, else prior calendar month. Two-part materiality (absolute OR percentage, via `AiMaterialityPolicy`) gates decomposition/escalation only — unconfigured policy reports every movement `unclassified`, never filters it. Driver decomposition (by counterparty) is exact in cash terms by construction: `drivers + unexplained_amount = total variance`, to the cent, always. `timing_vs_real_change_decomposition` declared `not_implemented` — AI-28 exports no reusable function to "ask" (`OPEN_QUESTIONS.md` #26). 6 tests. |
| AI-15 | Anomaly detection | **BUILT (Chunk 5)** | New: `lib/aiRuntime/workflows/ai-15-anomaly-detection/index.ts` | New: `models/ai/{AiAnomaly,AiDetectorHealth,AiAnomalySuppression}.ts` | New tools (all `internal_state`): `record_anomaly`, `confirm_anomaly`, `dismiss_anomaly`, `suppress_anomaly` | none new | OBSERVE, no write tool touches a financial document anywhere (asserted). Nine detectors across all six named families (Amount ×2, Counterparty ×2, Account ×1, Timing ×2, Journal pattern ×1, Ratio/trend ×1 — the last reads AI-14's latest trace). A.5's full precision budget: every anomaly ships `silent: true` until a detector clears `AI15_MIN_SAMPLE` (20) reviewed anomalies at >= `AI15_PRECISION_FLOOR` (50%) precision; falling short auto-disables the detector (one-way) and raises exactly one INFO item. `vendor_shares_bank_or_address_with_employee` declared `not_implemented` (`Vendor.ts` has neither field). 8 tests incl. the brief's own "single most important test" (a year of normal activity → zero anomalies). |
| AI-16 | Cash intelligence | **BUILT (Chunk 5)** | New: `lib/aiRuntime/workflows/ai-16-cash-intelligence/index.ts`. Reuses AI-03's `computeBankPosition()` verbatim; reads AI-05's/AI-06's own most recent `AiDecisionTrace.rawProposal` rather than re-deriving predicted payment dates or the due schedule | none new | none new | none new | OBSERVE, zero tool calls anywhere in its folder (asserted — recommends, never initiates). Fixed 30-day horizon. Non-INR currency with no `FxRate` is excluded from `position.total_available` and the forecast entirely, never summed in blind — `incomplete_reason` says why. `tax_payments` outflow declared `not_implemented` (no filing model). `opening + inflows - outflows = closing`, exactly, every day, proven directly. 6 tests. |
| AI-17 | Compliance readiness | **BUILT (Chunk 6)** | New: `lib/aiRuntime/workflows/ai-17-compliance-readiness/index.ts`, `lib/aiRuntime/compliance/computeReadiness.ts` — the same function AI-13's new `compliance` domain (`lib/aiRuntime/closeReadiness/domains.ts::checkComplianceDomain`) wraps, never a second, disagreeing readiness computation. Extracted `lib/aiRuntime/tax/taxSignals.ts` (`findTreatmentExceptions`/`findMissingEvidence`) out of AI-12 so both workflows read the identical signal | reads `models/ai/{AiComplianceProfile,AiTaxTransaction}.ts` (no new models) | none new (read-only; reuses AI-12's `run_tax_reconciliation` reconciliation definition directly, not through the tool layer, matching `closeReadiness/domains.ts`'s existing pattern) | none new | OBSERVE, zero tool calls (asserted — no `rt` used in `act()`). Obligation calendar generated per period: monthly obligations apply every period, quarterly only on a calendar-quarter-end month, annual only in December — a documented calendar (not fiscal-year-aware) simplification. Readiness (`ready\|at_risk\|blocked\|not_started`) per obligation folds in AI-12's own three-way/missing-evidence/treatment signals plus a configurable, generously-defaulted (21-day) deadline warning window. Registration gaps (an obligation/threshold with no matching registration) are always HIGH. `submissionCapability` always `"not_implemented"` — no `submit_filing` tool exists anywhere (asserted). 11 tests. |
| AI-18 | Audit / evidence intelligence | **BUILT (Chunk 7)** | New: `lib/aiRuntime/workflows/ai-18-audit-evidence/index.ts`, `lib/aiRuntime/audit/{citations,traceEvidence,decisionTrace,sampling}.ts`. Extends AI-21's `drillIntoAccount()` downward to documents (`ExtractedDocument.createdRecordId`) and approvals (`JournalEntry.approvalDetails`) — never a second trace implementation | New: `models/ai/AiEvidencePack.ts` (reproducible, citable pack per `{tenantId, packId}`) | New tools: `get_activity_log`/`get_decision_trace`/`build_evidence_pack` (read/analyse), `record_evidence_pack` (`internal_state`) | none new | OBSERVE. **A.2's citation rule is structural, not a prompt**: `makeClaim()` throws on an empty `citations[]` — an uncited claim cannot exist in this workflow's output, asserted directly. "No evidence found" cites the search performed (`{model: "Query", id: <description>}`), never a bare sentence. Decision-trace retrieval ("why did the system do this") is a reasonable-effort scan of the tenant's recent `AiDecisionTrace` rows for a `toolCalls[].result` containing the target id — documented as a scan, not a magic index, since no workflow's trace carries a structured record-touched field yet. Sampling is a seeded PRNG (mulberry32), reproducible from `{method, seed}` — proven by running it twice. Sweeps AI-21's own `unsupportedMaterial` lines by default. 9 tests. |
| AI-19 | Master data intelligence | **BUILT (Chunk 8a)** | Duplicate entities (vendor/customer/item) via AI-20's own matcher, reused directly — never a second implementation. Missing-critical-field gaps. Bank-detail-change detection (Employee.bankDetails/BankAccount — Vendor/Customer confirmed to have no bank field at all) raises `CRITICAL`, **places a hold** (`place_hold`, generic, reused by AI-27), and **no `release_hold` tool exists at any autonomy level** (asserted). Employee/vendor collision via name+email (the honest, buildable version — no bank/address field exists on Customer). Observed payment terms (closes AI-06's `early_payment_discount` gap). Bank details masked (last 4 digits only) everywhere including the decision trace. **Never writes Vendor/Customer/Employee/Product/InventoryItem** (source-grepped). `RECOMMEND`, +`place_hold` at `CONTROLLED_AUTONOMOUS`. `master_data.changed` events added additively to the Vendor/Customer/Employee/BankAccount routes. | `models/ai/{AiMasterDataSnapshot,AiMasterDataProfile,AiHold}.ts` | none (internal workflow only) | none | 7 tests incl. masking-everywhere and the mandatory false positive. `expiring_documents`/`vendor_bank_change_detection`(Vendor/Customer side)/`classification_inconsistencies` declared honestly `not_implemented`. |
| AI-20 | Intercompany / consolidation intelligence — consolidation itself `not_implemented` by design; related-party detection **BUILT (Chunk 6)** | **PARTIAL, deliberately** | New: `lib/aiRuntime/workflows/ai-20-related-party-detection/index.ts`, `lib/aiRuntime/relatedParty/detectRelatedParties.ts`. New: `docs/ai/AI-20-ARCHITECTURE-NOTE.md` — full design memo on why consolidation can't be built honestly today (`Organization.subdomain` **is** `tenantId`; two group companies are two tenants, and the context service structurally prevents cross-tenant reads — a security property, correct as-is), two options (single-tenant parent/child entity model vs. an explicit-consent cross-tenant service), and a recommendation (Option A, only if a tenant actually asks). `lib/aiRuntime/reconciliation/definitions.ts`'s `intercompany` entry stays `not_implemented`, reason now pointing at this memo | none new (reads `Customer`/`Invoice`) | none new | none new | Consolidation: `not_implemented`, by design, not a gap. Related-party detection: OBSERVE, proposes/merges/eliminates nothing. The brief's "Customer vs Vendor" is reinterpreted honestly for this codebase's actual schema — `PurchaseOrder`/vendor-bill `Invoice.partnerId` both ref `Customer` (Odoo-style unified partner, `CLAUDE.md` Known Issue #4; `models/admin/Vendor.ts` is an unrelated procurement-rating list with no GSTIN/PAN/address) — so matching is one `Customer` record in a sales role against a different one in a purchase role. Shared GSTIN/PAN → `certain`; shared address or same non-generic email domain + name similarity → `probable`; name similarity alone → `possible`, never `certain` (explicit false-positive guard, tested). Shared bank account declared `not_implemented` — no such field exists on `Customer` anywhere. 5 tests incl. no-cross-tenant-leak (structural, not a lucky filter) and the different-similar-named-companies false positive. |
| AI-21 | Financial statement intelligence | **BUILT (Chunk 6)** | New: `lib/aiRuntime/workflows/ai-21-statement-intelligence/index.ts`, `lib/aiRuntime/statements/annotateStatement.ts` — an annotation layer over `lib/accounting/reports.ts::buildPostedJournalReport()`, never a second figure; drill-down (`drillIntoAccount`) is a thin wrapper over AI-14's own `getAccountTransactionDetail()`, the shared service AI-18 (Chunk 7) will also consume | none new (reads `Account`/`Asset`/`BankStatement`/`TaxRate`/`AiSchedule`/`AiWorkflowRun`/`AiDecisionTrace` to attribute each account to its owning AI-22 reconciliation definition and AI-13 close domain) | New: `get_statement`, `run_statement_annotation`, `drill_into_account` (all read/analyse) | New "Statements" tab on `/finance/ai-operations` (does not touch `/finance/reports` at all, per the brief) | OBSERVE, no ledger-value write path anywhere in this workflow's folder (asserted, same source-grep pattern as AI-09/AI-13). Reuses AI-22 (reconciliation status per account, via the same account-selection queries each definition already runs — `not_covered` when nothing checks an account, never an invented accusation), AI-13 (evidence status + staleness, from `AiCloseState.domains[]` already computed), AI-14 (materiality + movement, from its own most recent trace — `not_available` when it hasn't run) and AI-28 transitively (AI-14's own drivers already call `evaluateCutoff()`). `unsupportedMaterial` — the headline output — is deliberately narrow: `material` AND a real, machine-detected `unreconciled` status, never raised for uncovered or immaterial lines. 6 tests incl. statement-totals-equal-trial-balance and balance-sheet-balances. |
| AI-22 | Continuous reconciliation controller | **BUILT (Chunk 4, build first)** | New: `lib/aiRuntime/reconciliation/{engine.ts,classify.ts,definitions.ts}`, `lib/aiRuntime/workflows/ai-22-continuous-reconciliation/index.ts`. Wraps AI-03's bank position (extracted to a reusable `position.ts`, safe refactor) and AI-10's asset tie-out verbatim — neither reimplemented | `models/finance/BankReconciliation.ts` (bank pair, unchanged); no new models — every other pair reads existing data | see AI-03 | see AI-03 | OBSERVE only. One engine, 12 registered definitions: 9 real (`bank`, `ap_control`, `ar_control_finance`, `fixed_assets`, `inventory`, `payroll`, `prepaid`, `deferred_revenue`, `suspense_clearing`) + 3 `not_implemented` with stated reasons (`tax`, `intercompany`, `processor_settlement`). `"reconciled"` is structurally unreachable with an unexplained item — a pure classifier (`classify.ts`), tested directly. Scope simplifications (AP/AR current-state-only, inventory's `asset_current` bucket, suspense's name-matching) recorded in `OPEN_QUESTIONS.md` #21. 5 tests incl. bank-numbers-equal-AI-03 equality assertion. |
| AI-23 | Journal review intelligence | **BUILT (Chunk 7)** | New: `lib/aiRuntime/workflows/ai-23-journal-review/index.ts`, `lib/aiRuntime/journalReview/{tenantBaseline,scoreJournalRisk,buildRiskInput}.ts`. Reuses AI-15's own detectors — `manual_journal_to_sensitive_account` and the weekend/backdated timing pair were extracted to `lib/aiRuntime/journalPatterns/{sensitiveAccountPattern,timingPatterns}.ts`, which AI-15's own workflow now wraps too (behaviour-preserving refactor, its pre-existing suite still green) — never a second detector list | none new | New tool: `score_journal_risk` (read/analyse) | none new | OBSERVE/RECOMMEND. Every dimension is scored against **this tenant's own posting history** (account-amount z-score, account-combination frequency, poster frequency), never a global threshold. `check_sod` (0.4) made real for preparer≠approver and reused directly. Recommendation (`auto_ok\|review\|escalate`) always carries named `reasons[]`, never a bare score. Cannot post, approve, or alter `voucherStatus` at any confidence — no such tool exists anywhere (asserted). "Round-number amount" (the brief's own listed dimension) was tried and removed — it flagged every ordinary ₹1000 sale, caught by this workflow's own false-positive test (`OPEN_QUESTIONS.md`). 8 tests incl. a month of ordinary journals producing near-zero flags. |
| AI-24 | Close evidence controller | **BUILT (Chunk 4, build third)** | New: `lib/aiRuntime/workflows/ai-24-close-evidence/index.ts`, `lib/aiRuntime/evidence/assertions.ts` — pure predicates over the same live data AI-13 reads, never a parallel re-derivation | `models/ai/AiCloseAssertion.ts` (new, first-class inspectable assertion records); reads `PeriodClosing`, never writes it | same as AI-13 | same as AI-13 | OBSERVE plus `create_task`/`resolve_task` (the latter a deliberate, reasoned exception to "no new write tools," `OPEN_QUESTIONS.md` #19). 10 machine-verifiable assertions. Evidence requests dedupe across repeated sweeps (upsert on `AiAttentionItem.dedupeKey`) and auto-resolve once the underlying data changes — **never** by unticking `PeriodClosing`. 5 tests. |
| AI-25 | Working-capital intelligence | **BUILT (Chunk 5, DIO unblocked Chunk 8a)** | New: `lib/aiRuntime/workflows/ai-25-working-capital-intelligence/index.ts`. Reuses `buildAgedPartnerReport()` (the same function `/api/finance/reports/aged` serves) for AR/AP balances and `buildPostedJournalReport()` for revenue/COGS — no second aging or reporting engine. **Chunk 8a**: DIO now computed whenever AI-11's `resolveInventoryAccountMapping()` resolves for the tenant, using `glBalanceForAccount()` (exported additively from AI-22's reconciliation definitions, now takes an optional `asOfDate` — never a second balance query) for the inventory account's period-end balance against the same COGS figure DPO already used | none new | none new | none new | OBSERVE, read-only. One stated formula (period-end balance / period revenue-or-COGS × days in period), used consistently for both the current and prior period, each with its own day count. DIO/CCC stay `not_computable` per-tenant only when that tenant's own inventory-account mapping is still ambiguous or absent (a live reason, not the old static one) — `OPEN_QUESTIONS.md` #21/#24. Driver decomposition (by customer/vendor) is exact in cash terms by construction, proven directly against a known-value fixture. 5 tests incl. a real DIO=56 unblock fixture. |
| AI-26 | Accounting policy intelligence | **BUILT (Chunk 8a)** | Policy registry (`AiAccountingPolicy`: configured vs observed, versioned). Real consistency sweep: a bill above the configured capitalisation threshold posted to an expense account (vs one posted to `asset_fixed`) → inconsistency with both sides cited, valued. Live, per-tenant "uncovered transaction type" scan across every action class that actually reads `AiMaterialityPolicy`. All 6 A.3-inherited policy gaps (Chunks 3-5) surfaced with **live evidence queried from this tenant's own data** (e.g. the real `semanticOverride.applied` rate on posted `JournalEntry`s), not the brief's static text repeated. `OBSERVE` only — **never writes `AccountingSettings` or `lib/accounting/smart-rules.ts`** (asserted). | `models/ai/{AiAccountingPolicy,AiPolicyFinding}.ts` | none (internal workflow only) | none | 6 tests incl. all-6-inherited-gaps-present and the mandatory false positive (consistent treatment → zero inconsistencies). |
| AI-27 | Duplicate & duplicate-payment intelligence | **BUILT (Chunk 8a)** | Cross-source scoring over bills (never modifies `lib/docIntel/duplicateCheck.ts` — its existing callers, AI-01's extract route and AI-06, assert unchanged behaviour): same-vendor+normalised-document-number, same-vendor+amount+date, file hash (via `ExtractedDocument`), the duplicate-vendor case (reusing AI-19's `findDuplicateEntities` directly), and a real "split" detector (two bills summing to a third). `certain`/`probable` → **place a hold** (reusing AI-19's `place_hold`) + side-by-side comparison. Expense self-duplicate and a bank-statement-pattern detector (informational only — 0.3 confirmed no definitive bank↔bill link exists besides AI-03's own reconciliation). Retrospective sweep quantifies the already-paid, recoverable amount. `RECOMMEND`, +`place_hold`. | `models/ai/AiDuplicateFinding.ts` | none (internal workflow only) | none | 8 tests incl. the mandatory 12-monthly-subscription false positive and a legitimate same-PO second instalment producing zero flags. |
| AI-28 | Cut-off intelligence | **BUILT (Chunk 4, build last)** | New: `lib/aiRuntime/workflows/ai-28-cutoff-intelligence/index.ts` | none new — reads `Invoice`/`PurchaseOrder.stockMoveIds`/`StockMove.effectiveDate` (a real ObjectId link, not the plain-string `sourceDocument` field) | none | none | RECOMMEND only, drafts nothing. Scope: only the highest-priority evidence row (vendor bill vs. `StockMove` receipt date) is implemented deeply; other transaction types report `evidence_unavailable` rather than assuming the posting date is correct — `OPEN_QUESTIONS.md` has the full scope note. Never proposes a back-dated post into a locked period (asserted against the real `TransactionLock`). 5 tests. |
| AI-29 | Audit / control monitoring | **BUILT (Chunk 7, extended Chunk 8a)** | New: `lib/aiRuntime/workflows/ai-29-control-monitoring/index.ts`, `lib/aiRuntime/controls/{types,engine,definitions}.ts` — mirrors AI-22's reconciliation architecture exactly ("one engine, many definitions") | New: `models/ai/AiControlResult.ts` (per `{tenantId, controlId, period}`, the trend history a `design_concern` verdict is derived from) | New tools: `run_control_tests` (read/analyse), `record_control_result` (`internal_state`); remediation reuses the already-registered `create_task` | none new | OBSERVE. 12 registered controls: 9 real (`approval_present`, `sod_preparer_approver`, `no_posting_into_locked_period`, `closed_period_still_postable` — 0.5's design finding, made real — `journal_documentation`, `override_logged`, `payment_against_approved_bill`, `master_data_verification`, and `bank_detail_change_process` — the last three all flipped from `not_implemented` in Chunk 8a once AI-19's real infrastructure (`JournalEntry.lineIds[].sourceId`, `AiHold`, `AiMasterDataProfile.bankChangeAlerts`) made them genuinely checkable. **Clarified Chunk 8b (0.3)**: `bank_detail_change_process` tests the hold/verification *process* over `Employee`/`BankAccount` changes only — it does not and cannot watch `Vendor`/`Customer` bank details, which have no such field to watch at all (that gap is AI-19's own separately-declared `vendor_bank_change_detection` `not_implemented`)), 1 partial (`approver_authority`, honestly scoped to a plausible-role check since `lib/org/rbac.ts` has no authority-tier concept), 2 `not_implemented` (`sod_permission_conflict`, `access_change_authorised` — `ActivityLog` carries no structured entity/action-type field to match against, only free text). `overall_control_health` structurally excludes `not_implemented` controls (asserted). `design_concern` (failure rate ≥20% over ≥5 tested items) and remediation tasks both use `create_task`'s existing dedupe-by-key upsert — proven to fire once across 5 consecutive failing runs, never self-closeable by the AI. 15 tests. |
| AI-30 | ERP operations intelligence | **BUILT (Chunk 8a)** | Health sweep: stuck drafts/pending-approvals/workflow-runs/tool-calls, dead-lettered `AiEvent`s (grouped), failed integrations, stale tax projections/FX rates, overdue schedules, orphan workflow runs (no matching trace), duplicate/looping run executions. **The only workflow with real autonomous repair** (`CONTROLLED_AUTONOMOUS`, idempotent/reversible only), bound to A.5's 4 permitted types via one shared `repairGate.ts` (retry cap, exponential backoff, "fails twice escalates, never retried"). **2 of 4 repair types wired live**: re-queue a dead-lettered `AiEvent`; refresh a stale tax projection (AI-12's own `rebuildTaxProjection`, unchanged). **2 declared honestly `not_implemented`**: orphan relink (no genuine dangling-reference-with-determinable-parent pattern exists in this schema — the generic primitive is built and tested standalone) and integration-sync retry (the only re-runnable operation, `testConnection()`, mutates a non-`Ai*` model, and the normal RBAC write path requires a human `userId` an autonomous hourly sweep never has — a real architectural finding, not a shortcut). Kill switch explicitly checked in `act()` before any repair (asserted). **No repair path ever writes a financial record** (asserted). | `models/ai/{AiOperationsRepairLog,AiOperationsFinding}.ts` | none (internal workflow only) | none | 7 tests incl. healthy-vs-broken fixtures and retry-cap/escalation. |

---

## Summary

- **BUILT** (real, tested, event-triggered AI runtime workflow shipped): AI-01, AI-02, AI-03, AI-04 (Chunk 2), AI-07, AI-08, AI-09, AI-10 (Chunk 3), AI-13, AI-22, AI-24, AI-28 (Chunk 4), AI-05, AI-06, AI-16, AI-14, AI-15, AI-25 (Chunk 5), AI-12, AI-17, AI-21, AI-20 (Chunk 6), AI-18, AI-23, AI-29 (Chunk 7), AI-19, AI-11, AI-27, AI-26, AI-30 (Chunk 8a) — **30 of 30, all workflows built.** AI-20 counts as BUILT for its related-party detection half; group consolidation itself stays permanently `not_implemented` by design (`docs/ai/AI-20-ARCHITECTURE-NOTE.md`), the same shape as AI-13 shipping with permanently `not_applicable` domains. AI-29 similarly counts as BUILT with 4 of its 12 controls permanently `not_implemented`; AI-30 similarly with 2 of its 4 permitted repair types `not_implemented` — same honest reasons throughout.
- **EXISTS** (fully built, would only need an AI layer on top): 0 — nothing else in this codebase is a complete match for any remaining workflow's full behaviour yet.
- **PARTIAL**: 0 of 30 — all 30 are now BUILT.
- **MISSING** (nothing found; will be built new): 0 of 30.
- **PENDING SPEC**: 0 of 30.

**The single most important cross-cutting finding**: `lib/docIntel/` (document extraction, LLM-backed field extraction, duplicate detection, draft-only bill creation) is a real, working, already-shipped AI feature that directly underlies **three** planned workflows (AI-01 ingestion, AI-19 master data via its Customer resolve-or-create, AI-27 duplicate detection). Any Chunk 2+ work on those three must extend this module, not build a parallel one — this is exactly the failure mode Part 9, item 1 warns against.

The second most important finding: **no internal domain-event bus exists anywhere** (verified by repo-wide grep — see `SYSTEM_INVENTORY.md`). Every workflow that the brief describes as "event-triggered, not click-triggered" needs the Phase 1 runtime's event bus to exist first, and that event bus needs inline `emit()` calls added at real business-logic call sites (invoice creation, bill approval, bank import, etc.) — none of which exist today, including Aupulens Studio's structurally-similar-but-practically-unused `dispatchEvent()`.
